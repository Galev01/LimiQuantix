# =============================================================================
# Quantix-OS mkinitfs Configuration
# =============================================================================
# Defines kernel modules and features to include in the initramfs.
# =============================================================================

# Features to include in initramfs
features="ata base cdrom ext4 keymap kms mmc nvme scsi squashfs usb virtio xfs"

# Additional kernel modules to load
# These are loaded explicitly in addition to auto-detected modules
kernel_modules="loop squashfs overlay vfat ext4 xfs"

# Additional files to include in initramfs
# Our custom init script handles A/B boot logic
